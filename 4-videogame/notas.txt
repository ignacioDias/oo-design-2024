No sería mejor tener un List<Gems> gems y listo?
int damage() {
    return WEAPON_DAMAGE + Σ(gems.damage)
}
Permita que cada arma soporte un número máximo de gemas, que puede variar según
el tipo de arma.
void addGem(gems) {
    if gems.size < weapon.maxGems;
        gems.add(gem)
}
Las gemas engastadas ya no podrán quitarse nunca.
void removeGem(gem) {
    if gem.uses > 0
        gems.remove(gem)
}
